{
  "name": "server",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app",
    "start": "node app",
    "db:start": "docker compose up -d",
    "db:setup": "docker compose up -d && npx prisma migrate dev && npx prisma db seed",
    "db:reset": "docker compose down -v && docker compose up -d && npx prisma migrate dev && npx prisma db seed"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "description": "",
  "dependencies": {
    "@clerk/express": "^1.7.65",
    "@prisma/client": "^7.3.0",
    "cors": "^2.8.6",
    "drizzle-orm": "^0.45.1",
    "express": "^5.2.1",
    "pg": "^8.17.2"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "nodemon": "^3.1.11",
    "prisma": "^7.3.0"
  }
}
